name: Daily Data Scraping & AI Sentiment Pipeline

on:
  schedule:
    # Berjalan otomatis setiap hari pukul 00:00 UTC (07:00 WIB)
    - cron: '0 0 * * *'
  # Memungkinkan pemicuan manual dari tab Actions di repositori GitHub
  workflow_dispatch:

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Scraper (Penarikan Data)
      env:
        APIFY_API_TOKEN: ${{ secrets.APIFY_API_TOKEN }}
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
      run: python 01_run_scraper.py

    - name: Run AI & ML Sentiment Pipeline (Proses standardisasi & sentimen)
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
      run: python 01_pipeline_data.py
